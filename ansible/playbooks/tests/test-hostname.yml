- hosts: masters
  become: yes
  gather_facts: no
  vars_files:
    - /etc/ansible/vRA8/ansible/host_vars/vra_user_host_vars.yml

  tasks:
    name: Set hostname
      shell: |
        hostnamectl set-hostname "{{ vm-hostname }}"